<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Font?>

<VBox xmlns="http://javafx.com/javafx/21" xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="org.example.controller.Page1Controller"
      styleClass="root">



    <!-- Header -->
    <HBox styleClass="header" alignment="CENTER">
        <padding><Insets top="20" right="30" bottom="20" left="30"/></padding>
        <Label text="🍊 Calculateur d'Azote pour Agrumes" styleClass="header-title">
            <font><Font size="24" name="System Bold"/></font>
        </Label>
    </HBox>

    <!-- Progress Indicator -->
    <HBox alignment="CENTER" spacing="30" styleClass="progress-bar">
        <padding><Insets top="15" right="30" bottom="15" left="30"/></padding>

        <VBox alignment="CENTER" spacing="5">
            <Label text="1" styleClass="progress-step-active"/>
            <Label text="Configuration" styleClass="progress-label-active"/>
        </VBox>

        <Label text="→" styleClass="progress-arrow"/>

        <VBox alignment="CENTER" spacing="5">
            <Label text="2" styleClass="progress-step-inactive"/>
            <Label text="Calcul" styleClass="progress-label-inactive"/>
        </VBox>

        <Label text="→" styleClass="progress-arrow"/>

        <VBox alignment="CENTER" spacing="5">
            <Label text="3" styleClass="progress-step-inactive"/>
            <Label text="Historique" styleClass="progress-label-inactive"/>
        </VBox>
    </HBox>

    <!-- Main Content -->
    <ScrollPane fitToWidth="true" VBox.vgrow="ALWAYS" styleClass="content-scroll">
        <VBox spacing="20" styleClass="content">
            <padding><Insets top="30" right="40" bottom="30" left="40"/></padding>

            <!-- Page Title - Centered -->
            <VBox spacing="5" alignment="CENTER">
                <Label text="Configuration de la Ferme" styleClass="page-title">
                    <font><Font size="20" name="System Bold"/></font>
                </Label>
                <Label text="Entrez les informations de base de la ferme et les valeurs initiales" styleClass="page-subtitle"/>
            </VBox>

            <!-- Two Column Layout for Farm Info and Variety -->
            <HBox spacing="20" fillHeight="true">
                <!-- Left Column: Farm Information -->
                <VBox spacing="15" styleClass="form-section" HBox.hgrow="ALWAYS" minWidth="0">
                    <Label text="Détails de la Ferme" styleClass="section-title">
                        <font><Font size="14" name="System Bold"/></font>
                    </Label>

                    <VBox spacing="8">
                        <Label text="Nom de la Ferme" styleClass="label"/>
                        <TextField fx:id="farmNameField" promptText="domaine sous massa"/>
                    </VBox>

                    <VBox spacing="8">
                        <Label text="Nom de la Parcelle" styleClass="label"/>
                        <TextField fx:id="parcelNameField" promptText="domaine"/>
                    </VBox>

                    <VBox spacing="8">
                        <Label text="Coordonnées" styleClass="label"/>
                        <TextArea fx:id="contactInfoArea" promptText="+212 6 45 1885 81"
                                  prefRowCount="3"/>
                    </VBox>
                </VBox>

                <!-- Right Column: Variety & Yield -->
                <VBox spacing="15" styleClass="form-section" HBox.hgrow="ALWAYS" minWidth="0">
                    <Label text="Variété d'Agrumes &amp; Rendement" styleClass="section-title">
                        <font><Font size="14" name="System Bold"/></font>
                    </Label>

                    <VBox spacing="8">
                        <Label text="Sélection de Variété" styleClass="label"/>
                        <ComboBox fx:id="varietyComboBox" maxWidth="Infinity"/>
                    </VBox>

                    <VBox spacing="8">
                        <Label text="Rendement Potentiel (tonnes)" styleClass="label"/>
                        <TextField fx:id="expectedYieldField" promptText="25"/>
                    </VBox>
                </VBox>
            </HBox>

            <!-- Single Column: Pratiques de Gestion -->
            <VBox spacing="20" styleClass="form-section">
                <Label text="Pratiques de Gestion" styleClass="section-title">
                    <font><Font size="14" name="System Bold"/></font>
                </Label>

                <!-- Pruning Intensity -->
                <VBox spacing="10">
                    <Label text="Intensité de Taille" styleClass="label"/>
                    <HBox spacing="30">
                        <RadioButton fx:id="lightPruningRadio" text="Légère" toggleGroup="$pruningGroup"/>
                        <RadioButton fx:id="mediumPruningRadio" text="Moyenne" toggleGroup="$pruningGroup"/>
                        <RadioButton fx:id="heavyPruningRadio" text="Sévère" toggleGroup="$pruningGroup"/>
                    </HBox>
                </VBox>

                <!-- Leaves Management -->
                <VBox spacing="10">
                    <Label text="Gestion des Feuilles" styleClass="label"/>
                    <HBox spacing="30">
                        <RadioButton fx:id="allRemovedLeavesRadio" text="Toutes Enlevées" toggleGroup="$leavesGroup"/>
                        <RadioButton fx:id="buriedLeavesRadio" text="Enfouies" toggleGroup="$leavesGroup"/>
                        <RadioButton fx:id="notBuriedLeavesRadio" text="Non Enfouies" toggleGroup="$leavesGroup"/>
                    </HBox>
                </VBox>

                <!-- Weed Management -->
                <VBox spacing="10">
                    <Label text="Gestion des Mauvaises Herbes" styleClass="label"/>
                    <VBox spacing="10">
                        <RadioButton fx:id="chemicalPlowingWeedsRadio"
                                     text="Désherbage Chimique ou Labours" toggleGroup="$weedsGroup"/>
                        <RadioButton fx:id="occasionalWeedsRadio"
                                     text="Prélèvements Occasionnels" toggleGroup="$weedsGroup"/>
                    </VBox>
                </VBox>
            </VBox>

            <!-- Action Buttons -->
            <HBox spacing="15" alignment="CENTER_RIGHT" styleClass="button-bar">
                <Button text="TEST NAVIGATION" onAction="#handleNext" style="-fx-background-color: red; -fx-text-fill: white; -fx-font-size: 20px; -fx-padding: 15 30;"/>
                <Button text="Enregistrer la Configuration" onAction="#handleSaveConfig" styleClass="secondary-button"/>
                <Button text="Suivant → Calculer" onAction="#handleNext" styleClass="blue-button"/>
            </HBox>

        </VBox>
    </ScrollPane>

</VBox>
